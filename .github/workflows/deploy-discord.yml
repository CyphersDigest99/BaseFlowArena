name: Deploy discord → discord.rhymenex.us

on:
  push:
    branches:
      - discord

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel deploy hook
        run: curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK_DISCORD }}"
